# Supabase — copie para .env.local e preencha com os valores do seu projeto
# (Project Settings → API). A anon key é segura para o cliente: o RLS protege os dados.
# Se estas variáveis ficarem vazias, o app roda em modo local (localStorage).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
